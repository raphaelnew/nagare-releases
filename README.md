# Nagare Worker releases

Downloads and the Sparkle appcast for Nagare Worker, the macOS menu bar app of Nagare. The source lives in the private repository raphaelnew/nagare; the worker-app-release workflow publishes the zip and the DMG to the rolling `downloads` release here and `appcast.xml` to this branch.
